# =============================================================================
# Environment Variables for Samah Store Production Deployment
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env (or copy-item on Windows)
# 2. Fill in all required values (marked with REQUIRED)
# 3. Generate secure secrets using commands below
# 4. NEVER commit .env to version control
#
# GENERATE JWT_SECRET (pick one method):
#   Linux/Mac:    openssl rand -base64 64
#   Windows PS:   [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Max 256 }) -as [byte[]])
#   Or online:    Use a secure random string generator (64+ chars)
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
POSTGRES_DB=samah_store
POSTGRES_USER=samah
# REQUIRED: Set a strong password (at least 16 characters)
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# -----------------------------------------------------------------------------
# JWT Authentication
# -----------------------------------------------------------------------------
# REQUIRED: Must be at least 64 characters for HS512 algorithm
#
# Generate on Windows PowerShell:
#   [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Max 256 }) -as [byte[]])
#
# Generate on Linux/Mac:
#   openssl rand -base64 64
#
JWT_SECRET=CHANGE_ME_GENERATE_WITH_COMMAND_ABOVE_MUST_BE_64_PLUS_CHARS

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# For same-origin deployment via reverse proxy, this can be left as default
CORS_ALLOWED_ORIGINS=https://samah-store.tech,https://www.samah-store.tech

# -----------------------------------------------------------------------------
# File Uploads (optional - has defaults)
# -----------------------------------------------------------------------------
# UPLOAD_DIR=/app/uploads
# UPLOAD_BASE_URL=/uploads

# =============================================================================
# SECURITY CHECKLIST BEFORE DEPLOYMENT:
# =============================================================================
# [ ] POSTGRES_PASSWORD is a strong, unique password
# [ ] JWT_SECRET is at least 64 random characters (generated with command above)
# [ ] .env file is NOT committed to git (check .gitignore)
# [ ] File permissions are restricted: chmod 600 .env (Linux) or secure on Windows
# =============================================================================
