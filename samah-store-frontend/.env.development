# =============================================================================
# DEVELOPMENT ENVIRONMENT
# =============================================================================
#
# How it works:
#   1. Frontend runs on http://localhost:5173
#   2. Backend runs on http://localhost:8080
#   3. Vite proxy forwards /api/* and /uploads/* to backend (see vite.config.js)
#   4. No CORS issues because proxy makes it same-origin from browser's view
#
# VITE_API_BASE_URL is empty so all API calls use relative paths (/api/*)
# which get proxied by Vite dev server to the backend.
# =============================================================================

VITE_API_BASE_URL=

# Vite proxy target (used by vite.config.js)
VITE_API_PROXY_TARGET=http://localhost:8080
